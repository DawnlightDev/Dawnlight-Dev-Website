[build]
  command = "npm run build"  # Adjust this to your build script
  publish = "public"         # Adjust this to your build output directory

[dev]
  command = "npm start"      # Adjust this to your development start script

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
